// Stub: shared constants — not used in this static portfolio build.
// This file exists to satisfy TypeScript module resolution for
// orphaned scaffold files (config/env.ts) that are not imported
// by any active component.

/** Cookie name used by the OAuth flow. */
export const COOKIE_NAME = 'session';

/** One year in milliseconds. */
export const ONE_YEAR_MS = 365 * 24 * 60 * 60 * 1000;
